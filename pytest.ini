[pytest]
# Force pytest-qt to use PyQt6 to match the project's requirement
# and avoid conflicts with other installed Qt bindings like PySide6.
qt_api = pyqt6

# Skip RDKit-dependent tests if RDKit is not available
markers =
    rdkit: tests requiring RDKit
